# The next round, computed — not chosen

> GENERATED by `scripts/opgraph-eig.mjs` from the shipped graph. Do not hand-edit:
> re-run it. Every figure is re-derived from tracked state on each run.

Graph state: **508 nodes** — 101 works · 246 procedure-claims · 45 relation-claims.

## The ranking

`score = affected × headroom × leverage / cost` — a stated proxy, not true EIG. The
statistics are the contribution; the ordering is an argument you may disagree with.

| # | action | affected | headroom | lev | cost | score |
|---|---|---:|---:|---:|---:|---:|
| 1 | A · second witness | 347 | 0.3 | 1 | 3 | **34.7** |
| 2 | B · basis upgrade | 217 | 0.15 | 1 | 4 | **8.1** |
| 3 | E · desk-check queue | 50 | 0.08 | 1 | 1 | **4.0** |
| 4 | D · atlas join | 24 | 0.05 | 2 | 1 | **2.4** |
| 5 | C · procedure-level relations | 42 | 0.1 | 2 | 4 | **2.1** |
| 6 | F · a silent culture | 0 | 0 | 2 | 5 | **0.0** |

### A · second witness

**Add one INDEPENDENT second witness to the highest-weight nodes.**

Every node in the graph is single-witness (246/246 claims, 101/101 works). The rubric multiplies a witness term that is therefore at its floor everywhere, which is why 137/347 weighted nodes sit within 0.10 of the 0.4 admission floor. This is the only action that can move that term at all.

### B · basis upgrade

**Replace derived `genre-norm` completeness bases with read evidence.**

217 of 246 claims rest on the generator's weakest default; only 29 rest on something read. The round deliberately under-claimed — this is the debt that created.

### E · desk-check queue

**Clear the flagged rows: unverified claims, retype-pending, unresolved edition PD.**

16 unverified · 12 retype-pending · 22/177 editions with unresolved public-domain status. PD status gates quotability, so this one has a direct downstream consequence.

### D · atlas join

**Link the works flagged `atlasNeeded` to their Confluence atlas slugs.**

24 works want an atlas counterpart. The join key already exists; this is the cheapest structural win on the board and it makes the two graphs answer each other.

### C · procedure-level relations

**Upgrade work-level relation-claims to procedure-level where evidence allows.**

3 of 45 relation-claims are procedure-level. "Where did this SEQUENCE come from?" is the one question this graph asks that no other resource does, and it is currently answered 3 times. The other 42 assert only that two works are related.

### F · a silent culture

**Open one culture the graph currently has zero coverage of.**

The data ceiling names them: Mesoamerican, sub-Saharan African, Mesopotamian, Shintō/Shugendō, Slavic. A zero is not a small number — it is a different kind of claim, and the graph says so itself.

> ⚠ affected=0 by construction: the nodes do not exist yet. The proxy CANNOT rank this action — a metric built on existing rows is structurally blind to absence. Ranked by hand, deliberately, and flagged as the proxy's known blind spot.

## What this metric cannot see

It ranks over rows that exist. An entire tradition absent from the corpus has
`affected = 0` and scores zero — so the proxy is structurally blind to exactly the
gap the data ceiling calls the graph's silence. Action F is therefore ranked by hand
and kept on the board rather than allowed to fall off it. A ranking that only ever
promotes filling in what is already started will never open a new shelf.
